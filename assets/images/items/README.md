# Starter Item Assets

These SVG files are the first project-local image asset pack for Hint Trap.

They are intentionally lightweight and transparent-background friendly so the game can still publish directly on GitHub Pages. Future AI-generated PNG or WebP assets can replace or sit beside these files without changing the engine, as long as level data points each item at the correct `image` path.
